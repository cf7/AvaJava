var addOdds = (x, y) -> if x%2 and y%2 both not 0 then return x+y else return 0 end;

var factorial = (n) -> if n <= 1 then return 1 else return n * factorial(n - 1) end;

var x = 3;
var y = 5;

factorial(addOdds(x, y)); // holding off on currying for now

